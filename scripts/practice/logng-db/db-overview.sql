
USE long_db;

SHOW TABLES;

DESCRIBE `countries`;
DESCRIBE `manufacturers`;
DESCRIBE `orders`;
DESCRIBE `phones`;
DESCRIBE `products`;
DESCRIBE `users`;

SHOW INDEXES IN `countries`;
SHOW INDEXES IN `manufacturers`;
SHOW INDEXES IN `orders`;
SHOW INDEXES IN `phones`;
SHOW INDEXES IN `products`;
SHOW INDEXES IN `users`;

SELECT *  FROM `countries`;
SELECT *  FROM `manufacturers`;
SELECT *  FROM `orders`;
SELECT *  FROM `phones`;
SELECT *  FROM `products`;
SELECT *  FROM `users`;


